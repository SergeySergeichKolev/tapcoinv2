# 🪙 TapCoin v2.0 — Telegram Mini App (Tap-to-Earn)

Кликер-игра для Telegram с SQLite базой данных, бустами, квестами и защитой от автокликеров.

## 📁 Структура проекта

```
tap-coin/
├── server.js          # Бэкенд (Node.js + better-sqlite3)
├── package.json
├── tapcoin.db         # SQLite база (создаётся автоматически)
├── public/
│   └── index.html     # Фронтенд
└── README.md
```

## 🚀 Как запустить

### 1. Установка зависимостей

```bash
cd tap-coin
npm install
```

### 2. Запуск

```bash
node server.js
# Или с токеном бота:
BOT_TOKEN=123456:ABC-DEF node server.js
```

Откроется на `http://localhost:3000`

### 3. Деплой на VPS (Jino)

```bash
# На сервере:
cd tap-coin
npm install
node server.js &

# Или через PM2:
npm install -g pm2
pm2 start server.js --name tapcoin
pm2 save
pm2 startup
```

**Важно:** файл `tapcoin.db` создаётся автоматически рядом с `server.js`. Все данные хранятся в нём и не пропадают при перезагрузке.

## 🎮 Фичи

- ✅ **SQLite база данных** — данные не теряются при перезагрузке
- ✅ Тап по монетке с анимациями и частицами
- ✅ **Система бустов** (Tap Power, Energy Tank, Energy Regen, Multi Tap) — как в Notcoin
- ✅ **Система квестов** (за тапы, монеты, уровни, покупку бустов)
- ✅ Система энергии (восстанавливается со временем)
- ✅ Таблица лидеров
- ✅ Уровни и лиги (Bronze → Silver → Gold → Diamond → Master → Legendary)
- ✅ **Анти-чит защита:**
  - Rate limiting на сервере
  - Проверка скорости тапов (макс 15/сек)
  - Лимит монет на батч относительно tap power
  - Отслеживание подозрительной активности
  - Логирование нарушений в БД
  - Клиентская проверка скорости
- ✅ Haptic feedback в Telegram
- ✅ Батчинг кликов (синхронизация каждые 2 сек)
- ✅ Работает офлайн для тестирования

## 📊 API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/user/:id` | Получить данные пользователя |
| POST | `/api/tap` | Синхронизация тапов |
| GET | `/api/leaderboard` | Таблица лидеров (топ 100) |
| GET | `/api/boosts/:userId` | Список бустов пользователя |
| POST | `/api/boosts/buy` | Купить/улучшить буст |
| GET | `/api/quests/:userId` | Список квестов пользователя |
| POST | `/api/quests/claim` | Забрать награду за квест |

## 🛡️ Бусты

| Буст | Эффект | Макс. уровень |
|------|--------|---------------|
| 👆 Tap Power | +1 к силе тапа за уровень | 20 |
| 🔋 Energy Tank | +500 к макс. энергии | 10 |
| ⚡ Energy Regen | +1/сек к восстановлению энергии | 10 |
| ✌️ Multi Tap | +2 к силе тапа за уровень | 10 |

Стоимость увеличивается с каждым уровнем (×1.8–2.5).

## 🎯 Квесты

Квесты за тапы, монеты, уровни и покупку бустов. Награда — монеты и бонус к силе тапа.
